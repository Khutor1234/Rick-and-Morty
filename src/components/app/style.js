import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    header: {
        color: '#fff',
        paddingTop: 40,
        textAlign: 'center',
        fontSize: 30
    },
}));

export default useStyles;